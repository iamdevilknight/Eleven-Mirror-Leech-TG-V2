name: Manually Deploy to Heroku

on: workflow_dispatch

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12
        with:
          heroku_api_key: "ecd1006f-b82b-4c77-b8dd-3ca4467b3a76" ## ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: "leech-mirror-eleven-1" ## ${{secrets.HEROKU_APP_NAME}}
          heroku_email: "DianaPrincessBotAc@gmail.com" ## "leechbotac@gmail.com" ${{secrets.HEROKU_EMAIL}}
          usedocker: true
          docker_heroku_process_type: web
          stack: "container"
          region: "us"
        ## env:
        ##  HD_CONFIG_FILE_URL: ${{secrets.CONFIG_FILE_URL}}
