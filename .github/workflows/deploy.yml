name: Manually Deploy to Heroku

on: workflow_dispatch

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12
        with:
          heroku_api_key: "8156a27d-af76-47c4-b663-939ddab053c4" ## ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: "leech-mirror-eleven-v2" ## ${{secrets.HEROKU_APP_NAME}}
          heroku_email: "dianaprincessbotac@gmail.com" ## "leechbotac@gmail.com" ${{secrets.HEROKU_EMAIL}}
          usedocker: true
          docker_heroku_process_type: web
          stack: "container"
          region: "us"
        ## env:
        ##  HD_CONFIG_FILE_URL: ${{secrets.CONFIG_FILE_URL}}
